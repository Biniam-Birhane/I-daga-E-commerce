const imageUrl = (pictureName)=>{

    return `http://127.0.0.1:8080/images/${pictureName}`;}

// const getImageUrl = (path) => {
//     return new URL(`../assets/img/${path}`, import.meta.url).href;
//   };
export default imageUrl
